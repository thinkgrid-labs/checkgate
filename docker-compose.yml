# docker-compose.yml — server-only variant
#
# Runs the Launchgate server image alongside managed PostgreSQL and Redis
# containers. Use this when you want Compose to own the dependencies but
# still keep each service in its own container (12-factor style).
#
# Usage:
#   docker compose up -d
#
# Override SDK_KEY before starting in production:
#   SDK_KEY=my-secret docker compose up -d

services:
  postgres:
    image: postgres:16-alpine
    container_name: launchgate-postgres
    environment:
      POSTGRES_USER: launchgate
      POSTGRES_PASSWORD: launchgate
      POSTGRES_DB: launchgate
    ports:
      - "5432:5432"
    volumes:
      - launchgate_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U launchgate -d launchgate"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: launchgate-redis
    ports:
      - "6379:6379"
    volumes:
      - launchgate_redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  launchgate:
    build:
      context: .
      dockerfile: Dockerfile.server
    image: launchgate:server
    container_name: launchgate-server
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://launchgate:launchgate@postgres/launchgate
      REDIS_URL: redis://redis:6379
      SDK_KEY: ${SDK_KEY:-}
      SESSION_SECRET: ${SESSION_SECRET:-}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  launchgate_pg_data:
  launchgate_redis_data:
