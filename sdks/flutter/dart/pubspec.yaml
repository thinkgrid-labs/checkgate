name: launchgate_flutter
description: Launchgate feature-flag SDK for Flutter — local evaluation via Rust FFI.
version: 0.1.0
homepage: https://github.com/your-org/launchgate

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
