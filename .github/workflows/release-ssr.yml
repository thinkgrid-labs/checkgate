name: Release SSR

# Triggered by version tags: v1.2.3 (shared convention with the SDK releases).
on:
  push:
    tags: ["v*.*.*"]

jobs:
  publish:
    name: Publish @checkgate/ssr to npm
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write   # OIDC token for npm provenance
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      # @checkgate/ssr has zero runtime dependencies — no install step needed.
      - name: Run tests
        working-directory: ssr
        run: node --test

      - name: Set version from tag
        working-directory: ssr
        run: |
          TAG="${{ github.ref_name }}"
          VERSION="${TAG#v}"
          npm version "$VERSION" --no-git-tag-version --allow-same-version

      - name: Publish @checkgate/ssr
        working-directory: ssr
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
